@model IEnumerable<Topic>
@{
    ViewData["Title"] = "Index";
}

@if(TempData["success"] !=null)
{
    <h2>@TempData["success"]</h2>
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Topic List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Topic" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus d-inline-block align-middle" style="font-size: 1.25rem; vertical-align:0"></i>
                Create New Topic
            </a>
        </div>
    </div>
    <br /><br />
    <table class="table table-hover" style="width:100%">
        <thead>
            <tr>
                <th>
                    Topic Name
                </th>
                <th>
                    Closure Date
                </th>
                <th>
                    Final Closure Date
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td>
                        @obj.Name
                    </td>
                    <td>
                        @obj.ClosureDate
                    </td>
                    <td>
                        @obj.FinalClosureDate
                    </td>
                    <td class="text-center">
                        <div class="border-start" role="button">
                            <a asp-controller="Topic" asp-action="Details" asp-route-id="@obj.Id"
                           class="btn btn-outline-info rounded align-middle" style="font-size:20px">
                                <i class="bi bi-info-circle"></i>
                                Detail
                            </a>
                            <a asp-controller="Topic" asp-action="Edit" asp-route-id="@obj.Id"
                           class="btn btn-outline-primary rounded align-middle">
                                <i class="bi bi-pencil-square align-middle" style="font-size:20px"></i>
                                Edit
                            </a>
                            <a asp-controller="Topic" asp-action="Delete" asp-route-id="@obj.Id"
                           class="btn btn-outline-danger rounded align-middle">
                                <i class="bi bi-trash align-middle d-inline-block" style="font-size:20px"></i>
                                Delete
                            </a>
                            <a asp-controller="Idea" asp-action="CSVFile" asp-route-id="@obj.Id"
                                class="btn btn-outline-primary rounded align-middle">
                                <i class="bi bi-cloud-arrow-down align-middle d-inline-block" style="font-size:20px"></i>
                                Downdload CSV
                            </a>
                            <a asp-controller="Idea" asp-action="ExcelFile" asp-route-id="@obj.Id"
                           class="btn btn-outline-primary rounded align-middle">
                                <i class="bi bi-cloud-arrow-down align-middle d-inline-block" style="font-size:20px"></i>
                                Downdload Excel
                            </a>
                            <a asp-controller="Topic" asp-action="ZipFile" asp-route-id="@obj.Id"
                           class="btn btn-outline-primary rounded align-middle">
                                <i class="bi bi-cloud-arrow-down align-middle d-inline-block" style="font-size:20px"></i>
                                Downdload Zip
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

